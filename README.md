# BasicCommandLineAPIClient
This is a simple GO command line tool that allows one to test HTTP requests and inspect responses. 
I developed BCLC mainly to aid my work. I wanted a small, yet somewhat robust API client tool that I could use from the command line to test little things quickly for web applications.